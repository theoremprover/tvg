int f(int x, int y)
{
    int erg = 0;
    int g = 1;

	g = 2;

    if (x < 0) g = -1;
	else
	{
		int z = x;
		z++;
	}

    for (int i = 1; i <= x; i++)
    {
		erg = erg + g * y;
		erg = erg;
    }
    return erg;
}

int main()
{
	return 0;
}
