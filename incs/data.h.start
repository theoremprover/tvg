#ifndef DATA_H
#define DATA_H

void opentrace();
void closetrace();

typedef struct {
	long line, column;
	long cnt; }
	COUNTER;

typedef struct {
	char sourcefilename[256];
	char output_filename[256];
	long num_counters;
	COUNTER counters[];
} SRCFILE;

/*INSERT_HERE*/

#endif
