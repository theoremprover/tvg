(set-option :smt.relevancy 0)
(set-option :produce-models true)

(declare-const answer (_ BitVec 64))
(declare-const high (_ BitVec 32))

(assert (= answer 0x0000007e7fffc0c0))
(assert (= high ((_ extract 31 0) (bvlshr () ())) )



(assert (= a #x0002))

(minimize a)
(check-sat)
(get-value (a))
(get-value (a2))
(get-value (a3))
